package com.dude.dms.brain.parsing.search

import org.assertj.core.api.Assertions.assertThat
import org.junit.jupiter.api.Test

internal class ExpressionLangTest {

    @Test
    fun searchParser() {
        /*assertThat(evaluate("1 == 1 and 2==2")).isEqualTo(true)
        assertThat(evaluate("true")).isEqualTo(true)
        assertThat(evaluate("1")).isEqualTo(1)
        assertThat(evaluate("123")).isEqualTo(123)
        assertThat(evaluate("[1, 2, 3]")).isEqualTo(listOf(1, 2, 3))
        assertThat(evaluate("[[1]]")).isEqualTo(listOf(listOf(1)))

        assertThat(evaluate("true == true")).isEqualTo(true)
        assertThat(evaluate("true == false")).isEqualTo(false)
        assertThat(evaluate("\"foo\" == \"bar\"")).isEqualTo(false)
        assertThat(evaluate("[1, 2] == [1, 2]")).isEqualTo(true)
        assertThat(evaluate("1 != 2")).isEqualTo(true)
        assertThat(evaluate("123 != 123")).isEqualTo(false)

        assertThat(evaluate("1 in []")).isEqualTo(false)
        assertThat(evaluate("1 in [\"\"]")).isEqualTo(false)
        assertThat(evaluate("1 in [1, 2, 3]")).isEqualTo(true)
        assertThat(evaluate("1 not in []")).isEqualTo(true)
        assertThat(evaluate("1 not in [\"\"]")).isEqualTo(true)
        assertThat(evaluate("1 not in [1, 2, 3]")).isEqualTo(false)

        assertThat(evaluate("1==1 and 2==2")).isEqualTo(true)
        assertThat(evaluate("1==1 and 2==3")).isEqualTo(false)
        assertThat(evaluate("1==1 or 2==2")).isEqualTo(true)
        assertThat(evaluate("1==2 or 2==3")).isEqualTo(false)
        assertThat(evaluate("not false")).isEqualTo(true)*/
    }
}